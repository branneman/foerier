# Handoff: Foerier — Quartermaster's tool (design system + core screens)

## Overview
Foerier is a household gear ledger for families who camp/trek: a **Depot** (where every piece of gear lives at home), **Trips** (gear lists, pack-out, unpack), and **Find** (offline "where is it?"). Dark theme is the default. This bundle contains the visual foundations, user flows, component sheet, seven core screens, and the full auth & account surface (Screens C).

## About the Design Files
The `.dc.html` files in this bundle are **design references created in HTML** — prototypes showing intended look and behavior, not production code to copy directly. Recreate these designs in the target codebase's existing environment (React, Vue, SwiftUI, native, …) using its established patterns and libraries. If no environment exists yet, choose the most appropriate stack (this is an offline-first mobile-primary app; local-first storage with background sync is a core requirement) and implement the designs there. Open each file in a browser to view it (`support.js` must sit next to them).

## Fidelity
**High-fidelity.** Colors, typography, spacing, and copy are final; recreate pixel-perfectly. The boards also contain annotation cards ("DECISIONS ON THIS SCREEN") beside each phone frame — these are rationale for the developer, not UI. The screens themselves are the framed artboards — 393px (Compact/Comfortable), 540px (Roomy), 900px (Split), 1024px (Desktop) — each marked with `data-screen-label`.

**Scope: the LATER tag.** Elements carrying the quiet mono `LATER` tag (dashed border, ink/faint — spec in Components §10) are drawn final and **decided, but not MVP**: the ledger group on Gear detail (story 33), all weight totals (story 16), `SAVED SLICES` in the Depot sidebar (story 34), and the whole Depot bulk bar (story 35). Build the MVP without them; their design does not change when they land. Two same-anatomy text variants: a **slice number** (`S8 · PIECES` on Find's per-person card) marks an MVP element that lands with a later slice — decided, not Later; **`API FIELD`** marks copy blocked on an endpoint field (the join-flow person names) — omitted until the field exists, never faked. Where a LATER element is a segment of a composite line (builder header/footer, gear meta), the MVP variant is drawn alongside. Everything untagged is MVP.

**This file has two writers, and regenerating it drops one of them.** The boards write the anatomy, copy and measurements. The **code** writes back everything recording what a slice actually did with a drawn decision — marked by a slice tag (`(S3)`, `(S3.5)`), by `retired`, by `drawn but not built`, by `landed in …` with a commit SHA, or by `redrawn round N`. They are concentrated in §2, §3a, §3b, §3c, §4a, §9 and §10, and there are enough of them that losing the set is not a small edit. A board redrawn from an older snapshot cannot know about any of them, so a regenerated README silently reverts them — and a reverted annotation reads as a *decision*, not as a loss.

So: **the repo copy at `docs/design/README.md` is authoritative, and a design round starts by re-seeding from it.** Preserve every code-authored annotation verbatim through a regeneration; never drop one because the current boards do not explain it — that is exactly the annotation worth keeping. If a board change genuinely contradicts one, say so as a conflict rather than resolving it silently: the code may need to change, or the board may be rediscovering something already settled. And **diff this file before committing a drop** — that is the net that catches whatever the re-seed missed.

## Design Tokens

### Color (dark default / light)
| Token | Dark | Light | Use |
|---|---|---|---|
| bg/base | `#151A15` | `#F0EBDD` | App background |
| bg/surface | `#1F2620` | `#F9F6EC` | Cards, group headers, sheets |
| bg/raised | `#28312A` | `#FFFFFF` | Hovered/active row, popover, segmented-active |
| ink/primary | `#E8E5D5` | `#232820` | Text, icons |
| ink/muted | `#97A08C` | `#6A7161` | Meta, secondary |
| ink/faint | `#6A7161` | — | Hints, column heads, ledger dates |
| ink/dim | `#5E6857` | — | Future/disabled chip text |
| rule | `#333C33` | `#D8D2BE` | Borders, dividers |
| rule/row | `#2A3129` | `#E3DECB` | Row dividers |
| rule/control | `#47523F` | `#BBB6A2` | Control borders, chip strokes (light values added S5 — parchment previously aliased all three tiers to `#D8D2BE`, which flattened every component leaning on the hierarchy) |
| accent | `#93BC9F` | `#35523F` | Primary actions, selection, links, active tab |
| brand/amber | `#E2A65B` | `#B4741F` | Logo handle, focus ring, ON TRIP marker, trip-only badge |
| status/not-packed | `#97A08C` | `#6A7161` | ○ empty circle + label |
| status/staged | `#D3A344` | `#8A6717` | ◐ half-filled circle + label |
| status/packed | `#7BB389` | `#40724B` | ● filled circle + check |
| status/attention | `#D98263` | `#A84B2F` | ▲ missing/lost, destructive text |

Derived fills: selected filter chip bg `#35523F`; input well `#0F130F`; status pill tints `rgba(211,163,68,.08)` staged, `rgba(123,179,137,.08)` packed, `rgba(217,130,99,.08)` attention; phase chip `rgba(226,166,91,.14)` bg + `rgba(226,166,91,.5)` border; selected row `rgba(147,188,159,.05)`; suggestion band `rgba(147,188,159,.07)` + border `#35523F`.

### Typography (self-hosted)
- **Bricolage Grotesque** 600/700 — display/titles
- **Spline Sans** 400/500/600 — UI text
- **Spline Sans Mono** 400/500/600 — all data: paths, counts, labels, chips (tabular figures; counts are load-bearing UI)
- **Delivery:** self-hosted **variable woff2** (one file per family, subset to Latin), hashed and **service-worker-precached** — works offline, no Google Fonts CDN, can't be blocked by privacy add-ons or proxies. `font-display: swap` with a **metric-matched fallback** `@font-face` (size-adjust/ascent-override) so the swap doesn't reflow. Preload only Spline Sans (primary UI font).

| Style | Spec | Use |
|---|---|---|
| DISPLAY | 28/34 Bricolage 700 (34 on desktop) | Screen titles |
| TITLE | 22/28 Bricolage 700 | Section/sheet titles |
| HEADING | 17/24 Spline Sans 600 (16/22 in dense list rows, 14 in tables) | Item names |
| BODY | 15/22 Spline Sans 400 | Default UI text |
| SECONDARY | 13/18 Spline Sans 400, ink/muted | Help, timestamps |
| DATA | 13/18 Mono 500 (11px in row meta) | Paths, counts, dates |
| LABEL | 11/14 Mono 600 CAPS, letter-spacing .08em (8–10px in chips/column heads, ls .05–.14em) | Chips, column heads, status |

### Spacing & grid
- Base **4px**; steps 4 · 8 · 12 · 16 · 20 · 24 · 32. Gutter by mode: 12 Compact · 16 Comfortable · 20–24 Roomy · 24 Split/Desktop. Content max-width 1120px, centred past 1200.
- Stack rhythm: gap ≈ 0.5–0.75 × the **next** element's line-height, snapped to 4 (20 before a title, 12 before a heading, 8 before body, 4 for tight text+meta pairs). Between cards 8–12; between sections 24–32.
- List row anatomy: side padding 16, vertical padding 12, title→meta gap 4. Two-line row = 70px; one-line min 56; packing rows ≥64. Whole row is the tap target.

### Responsive (five layout modes)
Shell breakpoints are **em-based media queries** (they track user font-size and zoom). Component internals adapt via **container queries** to the width they're handed — the same row/card renders 1-up or folded depending on its pane, not the viewport; no `@container` support fails open to single column.
| Mode | Range | Nav | Panes |
|---|---|---|---|
| Compact | <30em (<480) | Bottom tabs | Single column |
| Comfortable | 30–40em (480–640) | Bottom tabs | Single column |
| Roomy | 40–52em (640–832) | Bottom tabs | Single column, capped measure; cards 2-up, chips wrap, rows gain inline meta |
| Split | 52–64em (832–1024) | Icon rail (56px) | List + detail two-pane |
| Desktop | ≥64em (≥1024) | Labeled sidebar (216px) | Two/three-pane; centres at max-width 1120 past 75em (1200) |

### Shape & elevation
Chips/pills 999 · controls 8 · cards 12 · sheets 16 top. Rules 1px; chip strokes 1.5px. Dark theme elevates by lighter surface; light theme by rule + faint shadow. Focus ring 2px amber (`box-shadow: 0 0 0 2px rgba(226,166,91,.3)` + amber border).

### Touch
Targets ≥48px (status pill min 44), rows ≥56px, primary actions in thumb zone (bottom). Undo always visible after a state change.

## Status Grammar (hybrid — decided)
Never color alone: every state = **shape + fill-level + label**.
- **Packing progress = one circle, fill = state:** ○ empty stroke `#97A08C` NOT PACKED → ◐ left-half filled `#D3A344` STAGED → ● filled `#7BB389` with dark check PACKED.
- **▲ triangle = separate attention class** (`#D98263`): missing, lost, disagreement. Never used for progress.
- **RETIRED** = strikethrough, muted `#6A7161`.
- Status pill: 1.5px colored border, radius 999, tinted bg when non-empty, 12px icon + mono 9.5/600 label, padding 11px 14px — sits at the row's right edge (thumb side).
- **Journey rail** (container headers): chips for ⌂ HOME ✓ → STAGING ✓ → CAR ● → PACKED; past = bordered muted + ✓, current = inverted (bg `#E8E5D5`, text `#151A15`, ●), future = dashed border `#47523F`, text `#5E6857`.
- **Disagreement marker:** mono amber `#D3A344` line, e.g. `▲ IN CAR · 3 INSIDE NOT PACKED`.
- **Per-person piece circles:** initial = person, fill = that piece's status (filled sage = packed, half amber + amber border = staged, bordered `#47523F` = not packed). Sizes: 34px in rows, 28px in group headers, 24px in builder. In the **builder** circles mean inclusion, not status: all bordered; dashed border + dim = excluded.
- **Two worlds:** ⌂ HOME (muted `#97A08C`) vs ▸ ON TRIP (amber `#E2A65B`), everywhere. Depot never shows packing status; a trip row keeps its home path visible but muted.

## Voice: strict ledger
Terse, factual, numeric; mono for counts; sentence case for UI text; CAPS mono for labels. No exclamation marks, no cheerleading, no "oops".
Write: `3 left.` · `Packed · 14:32.` · `Offline. Saved on device.` · `Retired 2024. Kept in ledger.` · `No matches. 2 filters active.`
Never: "Almost there!", "Great job 🎉", "Say goodbye to this item?".

## Vocabulary guards
- **Piece** (capitalised) = one participant's copy of per-person gear on a trip — the only sense. Depot units of counted gear have **no identity, no name, no condition**; gear whose units genuinely differ is separate Single gear.
- **open** = the unresolved unpack state, everywhere (count line, filter pill, status pill, close card); never "out", which misreads as still away.
- **Unaccounted** = the read-back of `lost` (MVP): `▲ LAST SEEN: <trip>` + `RESOLVE`, identical in Find, Gear detail, and the Depot WHEREABOUTS column. `lost` writes nothing to the depot and keeps the gear's home slot.
- **Whereabouts words (ruled — Components §11):** the glyph names the **world** (`⌂` home, muted / `▸` on trip, amber / `▲` unaccounted, attention); the word after it names the **residence** where the surface answers residence (a path, or `LOOSE` for "lives nowhere" — the glossary term, written `⌂ LOOSE`), and the bare world (`HOME`, `ON TRIP`) where it only answers world. `HOME` never names a residence; `LOOSE` never stands alone as a world. One screen may correctly show both (Find: `⌂ HOME` chip beside a `⌂ LOOSE` line) — two questions, two words.
- **Tag** = flat, lowercase, `#` is drawn never stored; wire constraint (proposed for sync-protocol §4): lowercase `[a-z0-9-]`, 1–40 chars. No Tag entity, no rename — the pickers (§4a) are the only spelling defence. Trip-only gear is never tagged. `KIND` in UI always means the glossary Kind (`SINGLE · PER-PERSON · COUNTED`), never the containment trait (`ITEM`/`CONTAINER` — meta-line words only). "Slice" stays out of UI labels — the glossary has only "Saved slice" (Later); the phone control is the readout + `Sort and group` sheet.

## Screens / Views

### 1. Packing view — phone 393 (`Screens A`, labels "Packing by container", "Packing by person")
Purpose: mark gear staged/packed during pack-out, one-handed.
- Header: back link `‹ ALPS 2026` (mono 11 accent) left; sync state right (6px dot: sage SYNCED / amber OFFLINE + mono 9 label).
- Title `Pack-out` (28/34); count line `● 48/61 PIECES` vs `13 LEFT` (mono 11); 6px progress bar, radius 3, track `#333C33`, fill `#7BB389`.
- Controls: segmented CONTAINER | PERSON | ALL (40px tall, border `#47523F`, radius 8, active segment bg `#28312A`); `○ LEFT` filter pill (selected: bg `#35523F` + ✕).
- **By container:** group header (bg surface, 16/600 name + mono count `9/12`) carrying the journey rail + optional ▲ disagreement line; item rows: name 16/600 + mono meta (`SHARED · ×1`, `PER-PERSON · 1/3`), right = status pill or per-person circles. Trip-only items get amber bordered `TRIP-ONLY` tag (radius 3) and meta `NOT IN DEPOT`.
- **By person:** person group headers (28px circle + name, right `14/20 · 6 LEFT`); all-done person collapses to `● 20/20 — COLLAPSED`; participant who packs nothing is labeled `PARTICIPANT`.
- Footer bar (surface): hint `TAP CHIP = NEXT STATE · LONG-PRESS = ALL PEOPLE`, right `UNDO` in accent.

### 2. Depot browser — desktop 1024 (`Screens A`, "Depot desktop")
216px sidebar = **the app nav**, one spec shared by every desktop screen (redrawn R3, = §11's: logo, DEPOT 128 active bg `#28312A`, TRIPS 3, FIND, ACCOUNT avatar row pinned bottom, sync line in the sidebar beneath it — full measurements on the board's SIDEBAR ANATOMY card). R1–R2's Depot-scoped rail (ALL GEAR, PLACES, SAVED SLICES) is retired: it referenced no nav, left Trips/Find unreachable at ≥64em, and shipped at S3 as a second rail beside the shell's. **PLACES is retired outright** — Place is not a slice dimension (§8.5's ladder never lists it), no story browses by place; Find + the HOME column answer location. **SAVED SLICES stays story 34, LATER**, and lands with the slice bar (which owns narrowing), never the sidebar. Components §05's browse tree is tagged retired with it (no entry point). Main pane: title row (Depot, `128 GEAR · 214 PIECES`, search input 40px max-260 with `/` hint, `+ Add gear` accent button 40px); **slice bar, S3 state**: chips row (36px — the settled chip size everywhere; selected bg `#35523F` with ✕ reading dimension:value `TAG: #WINTER`, ghost add-chips dimension-only `+ TAG`; single-valued dimensions hide their ghost while active, TAG keeps its — tags AND together), right count `23 OF 128` + `CLEAR (2)`; arrange row beneath (GROUP BY segmented `NONE · KIND` 36px; sort on desktop = click a column head — **and the expanded row keeps its SORT options too (S3)**, because no column shows when gear was recorded, so column heads alone leave `NEWEST FIRST` unreachable at this width; hint `SEARCH + FILTERS COMBINE WITH AND`). S3 carries **Tag and Kind only**; the future-dimension ladder (Person/Ownership S4, Trip S7, Pieces-by-person S8, Status/Container S9, Outcome S10) is drawn dashed in Components §04 and never on screens. GROUP BY never offers TAG — deliberate: multi-valued tags don't partition a list. Optional bulk bar (tinted accent band: `3 SELECTED · MOVE · TAG · SET OWNER · RETIRE` — RETIRE in attention — **the whole band is LATER**, story 35: applying anything to a selection is the Later story; its verbs exist singly earlier); table radius 12: 8 columns `34px 1.45fr .5fr .6fr 1.45fr .95fr 44px 130px` = checkbox, GEAR (containment shows as the name's `5 INSIDE` suffix), KIND (**glossary Kind**: `SINGLE · PER-PERSON · COUNTED` — never ITEM/CONT., which misused the word), OWNER, HOME (path `ATTIC ▸ SHELF L-TOP ▸ CRATE B`), TAGS (plain mono text, ellipsis-truncates — deliberate at 44px density; the full set lives on gear detail), QTY (`×N` for counted only, `—` otherwise), WHEREABOUTS (`⌂ HOME` muted / `▸ ALPS · CAR` amber / `▲ ×1 TESSIN 2025` attention — unaccounted, MVP / `RETIRED` struck). Rows 44px; selected rows tinted; header row mono 8.5 caps; column heads sort (`GEAR ↑`). Keyboard: `↑↓ row · space select · / search · enter detail · click a column head = sort`. **S4 departures, and one spelling resolved.** `GROUP BY` now reads `NONE · KIND · OWNER` — the boards draw the control at S3's ship state and never at S4's, so the slice settled it; story 13's criterion asks for grouping by Person and Ownership, and grouping by the *register* is the only partition that names a Person while still filing the shared pool. Grouping is a small table beside the dimension table for that reason, which also turns "GROUP BY never offers TAG" from prose into the fact that a grouping needs a single bucket per gear and Tag has none. The **OWNER column is filled** (it read `—` from S3), and the owner spelling is **`PERSONAL E`** — the boards say `PERSONAL E` on Depot rows and `PERSONAL · E` on Packing rows, and this resolves to the Depot's, because the Depot is what S4 ships and Packing's `·` is that screen's separator between owner and count. One selector (`shared/`'s `ownerLabel`) serves the column, the row meta and gear detail, so they cannot disagree; an absent ownership register reads `SHARED`, which no board draws otherwise. The dashed future-dimension ladder in Components §04 is **down to four rungs** — Trip S7, Pieces-by-person S8, Status/Container S9, Outcome S10.

### 3. Depot — phone 393 (`Screens A`, "Depot phone")
Logo + avatar header; title; 48px search; horizontally scrolling filter chips (36px, S3 set: `TAG: #WINTER ✕` + ghosts `+ TAG` `+ KIND`); count line `9 OF 128 · CLEAR (1)` with the **arrange readout** right (`NAME A→Z ▾`, grouped: `KIND · NAME A→Z ▾`) — GROUP BY and SORT collapse behind it on phone, opening the sort-and-group sheet (rows 40+, current = `● NOW`, SET PHASE anatomy); a grouped frame is drawn (`Depot phone — grouped by kind`): group header = Packing's header minus the journey rail (surface band, 16/600 name + right mono count). Rows never show tags. 2-line rows (name + right whereabouts chip text; meta = owner/path/qty); container rows get `›`. 56px FAB (radius 16, accent bg, `+`) bottom-right, 74px above the 3-tab bar (DEPOT/TRIPS/FIND, active = sage icon + 2px top border). Keep 76px bottom list clearance for the FAB. Slice persistence: sort + group persist per device; filter chips + search reset on a fresh start, survive navigation; the shipped `4 MATCHES` search read becomes `4 OF 128` when the bar lands (one count line — search and filters AND).

### 3a. Depot — Roomy 540 & Split 900 (`Screens A`, "Depot roomy", "Trips roomy — cards 2-up", "Depot split")
**Roomy 540:** same bottom-tab shell as phone; gutters 20; filter chips wrap onto two lines instead of scrolling; count line keeps the arrange readout (the expanded GROUP BY row appears only ≥600px container — desktop's); rows go one-line with inline meta (name · owner/path/qty · whereabouts right); Trips cards render 2-up (see excerpt frame). **Split 900:** 56px icon rail (logo mark top, DEPOT active on raised 40px square; **ACCOUNT = 22px avatar pinned bottom above the sync dot** — added R3, muted `#47523F`/`#97A08C` like the phone header's, same 40px-square target; open, the square raises `#28312A`, the circle goes accent, and Account replaces the two panes as a single pane; sync dot bottom) + 308px list pane (folded 2-line rows via container query; chips + count + readout as phone; selected row tinted `rgba(147,188,159,.05)` with 2px accent inset edge) + gear-detail pane (title 24, MVP meta `ITEM · SHARED · ×2`, settled tag chips + `+ tag` ghost, whereabouts card, COUNT group with split chips — no per-unit rows, Move/Edit/RETIRE).

### 3b. Add gear (F1) — phone 393 fresh + mid-sitting / Roomy 540 / Split 900 (`Screens A` §06) — redrawn round 2
A screen, not a sheet — confirmed against the sheet alternative: the OS keyboard owns the lower half for a whole sitting, the Home picker stacks on it as the only sheet, and Split renders the form in the detail pane with the list kept. Entry points stay §2's `+ Add gear` and §3's FAB. **Order = the ledger line being written:** title `Add gear` (+ right mono session count `7 RECORDED` once recording) · NAME (48px well, focused on open, the only always-required input) · KIND segmented 48px `SINGLE · PER-PERSON · COUNTED` · **Owned count** — only while Counted, inserts below Kind (nothing at or above the thumb moves), stepper-flanked mono well that **opens empty and gates the CTA** (a silent ×1 is a wrong ledger line) · HOME row (bordered 48px, defaults `Loose`, opens §3c; **carries over between records within a sitting** — a depot is recorded shelf by shelf; a fresh entry starts at Loose) · **RECORDED AS** segmented `ITEM · CONTAINER` + fact `CONTAINERS HOLD OTHER GEAR · FIXED WHEN RECORDED` — the trait sits last: the rarest decision and the only irreversible one, beside the CTA; round 1's checkbox is retired (a checkbox reads as a setting) · CTA `Add gear` (disabled while a required input is empty; label constant) · mono fact `RECORDED ON THIS DEVICE · SYNCS IN THE BACKGROUND` — no failure state: one local `gear.recorded` op carrying every field. **After Add the screen stays** (round 1's navigate-to-detail is retired): name clears and keeps focus — return key records, so the batch loop is type → return → type — Kind/count/trait reset, Home persists, and a confirmation line appears under the title (`RECORDED · GAS CANISTER 450 G → CRATE B`); tapping it opens the record's detail. **UNDO is drawn but not built (S3), and is blocked on story 36 rather than wrong:** it is specified as "removes the op", which an append-only log that may already have pushed cannot do, and story 36 rules out the only compensating op by name ("does not leave the Gear marked, Retired") while forbidding a reversal that gets weaker because time passed — which a before-first-push retraction would be. Revisit when story 36's design phase lands. Roomy centres a 448 measure; Split keeps the list. **S4 adds an `OWNER` row after `HOME`, and it is a departure** — the board's order is settled and reasoned and carries no owner. Taken because without it the only route to attributing gear is one gear-detail visit per item, and the Depot's bulk `SET OWNER` band is story 35, `LATER`: a household attributing a two-hundred-item depot would make two hundred screen visits, and S4's own "personal gear stops being everyone's problem" would fail on the first day of real use. It is the same 48px bordered row `HOME` uses, opening an owner-picker sheet (`Shared` first, then People alphabetically, then a dashed `+ New person` — the Home picker's create-while-picking rule, since discovering mid-sitting that a Person was never recorded would otherwise mean leaving the form). It **carries over between records** and resets to `Shared` on a fresh entry, for the board's own `HOME` reason: a depot is recorded shelf by shelf, and a shelf in a bedroom is one person's. Owner is also one of the five shared attributes the domain model lists (home, owner, kind, tags, weight) and was the only one F1 omitted. Still one `gear.recorded` carrying every field — and leaving the row at `Shared` writes **no** ownership register, because absence already reads `SHARED` and a register nobody set would move the gear's `NEWEST FIRST` position for nothing. Recorded per the precedent set by §3b's own `UNDO` note and §3c.

### 3c. Home picker sheet (`Screens A` §07) — redrawn round 2
One bottom sheet shared by Add Gear and gear detail's `MOVE`; **two modes**. **Pick mode** (default): every row a bare tap target — selection closes the sheet; **Loose first**, two-line (meta `NO RESIDENCE — THE DEFAULT`); the current home is marked `● NOW` (SET PHASE anatomy — new this round; MOVE can't show where the gear stands without it); Places as ⌂-prefixed sentence-case rows with containers nested beneath, indent 16px/level **capped at two levels below the Place** (was three) — deeper rows keep the cap indent and carry their skipped ancestry as a meta line under the name (GearRow anatomy; round 1's inline parent prefix is retired) · dashed `+ New place` (creation stays in the pick path — a new shelf enters mid-sitting, and a place created while picking is **selected immediately**; the empty household reads `No places yet. Gear can stay loose, or live in a place — usually a room.` above Loose `● NOW` + `+ New place`) · ghost Close. **Edit mode** (quiet mono `EDIT` top right → `DONE`): selection suspends; Place rows gain RENAME (inline input + Save/Cancel) and REMOVE; container rows and Loose dim — containers are gear, renamed from their own EDIT. REMOVE always confronts the loose-count first (`4 pieces of gear become loose.`; an empty Place reads `0 pieces…`; bordered-attention button, never filled red). **MOVE:** context line `MOVING CRATE B · 5 INSIDE RIDE ALONG`; the moved gear and its whole subtree are **absent at any depth** (invariant 3) with one footer line saying so (`CRATE B AND EVERYTHING INSIDE IT ARE NOT OFFERED.`) — settles the conflict with Components §05's old blocked-rows mock, which is retired. Plain gear is never offered. Selection moves and closes. **MOVE confirms first (S3)** — the drawn "UNDO per the global rule" has no global rule to lean on while story 36 is Later, and a mis-tapped destination in a nested picker is otherwise unrecoverable without re-navigating; the primary stays accent, since nothing is destroyed by a move. Add Gear does not confirm: gear that does not exist yet has no prior state to lose.

### 4. Gear detail — phone 393 (`Screens B`, "Gear detail")
Header `‹ DEPOT` + sync. Title = gear name; meta mono `ITEM · SHARED · ×2 · 1.9 KG EACH` — the weight segment is tagged **LATER** (story 16); the MVP line is `ITEM · SHARED · ×2` (variant drawn beside the frame). **Tag chips — settled** (two frames disagreed; this wins everywhere): lowercase, mono 10.5, 32px, bordered `#47523F`, `#` drawn never stored; trailing dashed `+ tag` ghost is the one edit affordance on this read screen and opens the picker (§4a) — ✕ lives in the picker, not on the chips; a gear with no tags shows the lone `+ tag` ghost; removal confirms nothing (one op, instantly reversible).
- **Whereabouts card** (surface, radius 12): stacked rows `⌂ HOME SLOT` (label mono 8.5 faint; path mono 12) with `×1 THERE`, and `▸ ON TRIP — ALPS 2026` (label amber) with `×1 OUT` amber; footer hint `SPLIT COUNT — BOTH TRUE AT ONCE. HOME SLOT IS KEPT WHILE OUT.` An unaccounted split reads `▲ UNACCOUNTED · LAST SEEN: TESSIN 2025` + `RESOLVE` (variant beside the frame) — the same read as Find and the Depot column.
- **COUNT** group (right `×2 OWNED`): one split-whereabouts quantity line — chips `×1 ⌂ CRATE B` (neutral) + `×1 ▸ ALPS 2026` (amber) — with hint `COUNTED GEAR HAS NO PER-UNIT IDENTITY — UNITS THAT DIFFER ARE SEPARATE SINGLE GEAR.` No per-unit rows, no condition, no purchase year anywhere.
- **LEDGER** group (right hint `APPEND-ONLY`; tagged **LATER** — story 33, derived from the change log; keep exactly as drawn when built): mono rows `2026-08-19  PACKED → DUFFEL 90 L · BY M`, `LISTED ×1 · ALPS 2026`, `MOVED → CRATE B`, `BACK FROM VOSGES · OK`.
- Action bar (surface): bordered 44px MOVE and EDIT buttons; RETIRE right-aligned as attention-colored text (never a filled red button).

### 4a. Tag pickers (`Screens B` §01B) — S3
The only place a tag's spelling is ever decided: **there is no Tag entity** — the vocabulary is derived from what is applied, and **there is no rename, ever** (settled; a misspelling is fixed by remove + apply). One anatomy, two modes. **Gear-detail sheet** (`+ tag`): ON THIS GEAR chips with ✕ · input well with fixed `#` prefix · IN THE DEPOT rows **with counts** (`#cooking 14` — near-duplicates become visible at the moment they'd be created) · visibly distinct accent `+ CREATE #…` row. **Slice-bar mode** (from the `+ TAG` ghost chip; popover on desktop, same sheet minus ON THIS GEAR and + CREATE on phone): picks from what exists, **never creates**. Ghost add-chips are dimension-only (`+ TAG`) — a picker opens; the component sheet's old value-carrying ghost (`+ TAG: #WINTER`) was wrong and is retired. Input normalisation **is the op payload** — landed in [`sync-protocol.md`](../sync-protocol.md) §4.3 as `TagString` (`84ecd9b`), which also states the half this board cannot: **authoring normalises, readers never enforce**, so a non-conforming tag folds exactly as received. Lowercase, `[a-z0-9-]`, 1–40 chars, spaces become one hyphen, stored without `#`. **Two halves in code** (S3): `normalizeTagInput` for a live field, which keeps the hyphen a trailing space became so `cook set` does not collapse to `cookset` mid-word, and `normalizeTag` for the payload, defined in terms of it. Trip-only gear is never tagged (invariant 9) — neither picker ever mounts on it; stated now because trip-side screens reuse this chip and picker from S7.

### 5. Trips + Gear list builder (`Screens B`, "Trips" phone / "Gear list builder" 1024)
**Trips phone:** one active trip card (surface, radius 12): `▸ Alps 2026` + phase chip `PACK-OUT · DAY 2` (amber tint), dates mono, 22px people circles, progress line + bar, full-width 48px accent CTA `Continue pack-out`. **Dates are optional (story 5)** — without them the meta row simply drops (variant card beside the frame); the day count runs from the phase change, not from dates. Drafts = dashed-border card (`DRAFT · 0 GEAR LISTED`, `BUILD LIST ›` link). CLOSED group = ledger rows (muted name, `JUL 2025 · 54 PIECES · 1 LOST` with LOST in attention, `REOPEN` accent text + `›`). Tab bar, TRIPS active.
**Phases (story 32):** `DRAFT → PACK-OUT → ON TRIP → UNPACK → CLOSED`, set by a quartermaster, never by dates or counts, movable in **both directions** ("we had left" → the duffel is still in the hall). The CTA is a suggestion, not the only route: tapping the phase chip opens a SET PHASE sheet — five rows, current marked `● NOW`, any row tappable, backwards included.
**S6 ship state (before Entries exist) — drawn at `Screens B` 02A.** A board's CTA copy lands on the slice that builds its destination: `BUILD LIST ›` arrives with the builder, `Continue pack-out` with the packing view. Until then no card carries a button or verb link (the shipped `OPEN ›` is retired — it spent the strongest element on the flattest verb); the interim affordance is the closed row's own `›` and the whole card is tappable. In the progress slot, a per-phase **NEXT line** — a permanent obligation that survives the progress line and sits above it (full-weight five-element card drawn as a variant): `NEXT — BUILD THE GEAR LIST` (Draft) · `NEXT — PACK THE LIST` (Pack-out; redrawn from `PACK IT`) · `NEXT — SET UNPACK WHEN BACK` (On trip; redrawn — it names the actual control) · `NEXT — RESOLVE EVERY ENTRY, THEN CLOSE` (Unpack) · none (Closed). The line appears on every non-closed card, drafts included (shipped active-only; redrawn — the redundancy argument dies at `DRAFT · 14 GEAR LISTED`). N Active trips render as N identical cards, no primary-trip treatment. The middle band is named **PLANNED** (holds drafts and unrecognised phases — `DRAFTS` would misname the latter); only CLOSED draws a header today. `TRIPS 0` in the sidebar is blessed — the count is the size of the list the row opens, as `DEPOT 0`. The Trips list gains the Depot's 56px FAB as `+ NEW`'s drawn control.
**Trip screen `/trips/:id` (S6):** the builder header minus everything needing Entries — title is the name alone, no count, no `Start pack-out`; the empty region reads `0 GEAR LISTED.` + `The gear list is built from the depot.` — a permanent domain fact, never release meta-text, never a dead affordance; the same empty state gains the add affordances when the builder lands (drawn at 393 and 1024). **Participants live on the resting screen, not in EDIT** (redrawn — the shipped two-commit-model EDIT and its disclosure line are deleted): gear detail's tag-chip pattern — 22px circles + a dashed `+` ghost open the picker, writes land at once, removal never confirms. EDIT covers name and dates only, one commit model (Save/Cancel). **Participants picker:** rows in People-screen order, multi-select membership marked `PARTICIPANT ✓` (the builder's `IN LIST ✓` grammar; `● NOW` stays single-select's word), dashed `+ NEW PERSON` records and selects at once. **`/trips/new` is a screen, not a sheet** (the picker must stack on it; Add gear's shape): Name (only required input, focused) → dates (optional) → Participants → `Create trip` gated on name; Enter creates at desk widths. **Dates are the OS picker** — an external surface, the passkey-sheet rule; the boards draw dates only as output. No end-before-start guard (two devices may write both ends legitimately): a reversed range is **reported, never prevented** — `SEP 02 → AUG 14 · ▲ ENDS BEFORE IT STARTS`, day count suppressed while reversed. A stored non-`YYYY-MM-DD` date renders an empty input while the value survives; EDIT states it: `▲ STORED AS "…" — PICKING A DATE REPLACES IT`. **Unrecognised phase** (older build folding a newer one): chip draws the raw value verbatim, lowercase, quiet bordered — no amber (never over-state Active), no ▲ (the data is right, the build is old; nothing to resolve); files under PLANNED, no NEXT line; SET PHASE marks no row and states `● NOW — portaging` above five tappable rows, so no trip is stranded. **Tapping the `● NOW` row writes nothing and closes** — a redundant write would reset the chip's DAY count to a visible lie; the row stays undimmed (dim means future or disabled; NOW is satisfied). The SET PHASE sheet titles `SET PHASE` alone — the chip tapped is the trip's own.
**Two S6 departures decided in code, because no board draws the surface (design round, `Screens B` 02A).** Recorded per the §3b/§3c precedent. (1) **The FAB is withheld at Desktop, and the title row carries a `New trip` control instead** — no artboard draws Trips at 1024, and the FAB the board *does* give the list is offset from the bottom edge to clear the 3-tab bar, which Desktop replaces with a sidebar. So it mirrors `Depot` exactly: the control is rendered at `!isDesktop` and `+ NEW` takes the title row's trailing slot above it, as `+ Add gear` does there. Both carry the accessible name `New trip`; the `+` is decoration. (2) **The band above the title is decided in one hook**, on the four screens reached from a destination (`Trip`, `NewTrip`, `GearDetail`, `Account`). The **sync line is drawn at Split and only there**: `AppShell` states the status in words in the phone header and in the Desktop sidebar, and draws a bare 6px dot on the Split rail with the words on an `aria-label`, so Split is the one mode where nothing legible says it — which is exactly what §05's `Depot split` and §06's `Add gear — split 900` draw, `● SYNCED` in the pane's own band beside a rail that ends in a dot. The **back link is drawn unless its destination is already on the page**: never at Desktop, where `Trip screen — S6 desktop` puts `TRIPS` and the sync line in the 216px sidebar and neither in the main column, and at Split only for a screen that is not a pane — `Depot split` contains no `‹` at all, because the Depot list is drawn beside the detail. `Gear list builder` draws `‹ TRIPS` at 1024 and is a bare pane with **no sidebar**, which agrees rather than conflicts. The rule is one hook, `useScreenHeader`, taking a `splitPane` placement; [`frontend-design.md` §3.3](../frontend-design.md) states it and names the three screens that do not yet follow it. **Shipped inverted in the design round and corrected in review**: the first cut drew the back link at Split and withheld the sync line there, which also printed a second `SYNCED` under `AppShell`'s own header on a phone. `app/src/shell/screenBand.test.tsx` is what now counts the composed page — the per-screen suites render without the shell and can only ever prove half of it.
**Open — the boards give the FAB two offsets, and the code follows the Depot's.** `Screens A` draws `bottom:74px` three times (`Depot phone`, `Depot phone — grouped by kind`, `Depot roomy`); `Screens B` 02A's `Trips — S6 ship state` draws `bottom:86px`, once. Nothing beneath it differs — both boards draw the same 3-tab bar (`padding:9px 0 12px`, 19px icons, 8px labels above them) and the same 56px FAB, inset by the mode's own gutter — so the 12px reads as drift rather than as a decision about Trips, and a FAB that jumps between two tabs of one bottom bar is a defect. `Depot.module.css` and `Trips.module.css` therefore both ship `bottom: 4.625rem` = 74px, which is the shipped tab bar's 56px minimum (`AppShell.module.css` `.tabs`) plus 18. Stated, not settled: whichever round next redraws a FAB should say which number the boards mean, and if it is 86 then all four frames move together.
**Open — a second fact-line drift, closed nowhere.** `NewTrip.module.css`'s `.fact` ships `NAME IS THE ONLY REQUIRED INPUT` uncentred, though `New trip`'s own frame draws it `text-align:center`, and `Add gear`'s matching fact line agrees on both its 393 frames — but `Add gear — split 900` draws that same line inline, uncentred, so the boards contradict themselves rather than agreeing against the code. Unlike the three drifts closed below, there is no single rule to adopt: centring `.fact` would need a width gate no frame settles.
**Three drifts from 02A's own frames, closed in review rather than recorded as departures.** Each was the code differing from a frame that does draw the surface, with no argument behind the difference. (1) The trip card's NEXT line was `--color-ink-faint`; 02A draws it `#97A08C`, the same `--color-ink-muted` as the dates beside it, so the card's one instruction had been quieter than the metadata — and only in the dark theme, since the parchment palette maps both tokens to one value. (2) The dashed card's name was `--text-heading` (17px) like the Active card's; 02A draws it at 15px, and size is part of the quieter encoding the dashed outline and the plain-mono chip already carry. (3) `New trip`'s CTA was not pinned to the bottom, although the frame draws a `flex:1` spacer above it and the screen's own docstring claimed the thumb zone; three fields do not fill a phone, so the primary sat mid-screen on exactly the device the thumb zone is for. **`AddGear` still has (3)** — its board pins the CTA too, by a `flex:1` field region rather than a spacer, which is a larger change than one declaration and belongs to a round that touches the Depot's surfaces rather than the Trips ones.

**Builder 1024:** header `‹ TRIPS` + `Alps 2026 — gear list` (Bricolage 24) + dates/people + right `61 PIECES · EST 48.2 KG` (weight tagged LATER) + accent `Start pack-out`. MVP variants of the header and footer — no dates, no weight — are drawn under the frame. Two panes (`440px | 1fr`):
- Left "FROM THE DEPOT": search + filter chips; 40px rows (name + home path) with right `+ ADD` (bordered 32px) or `IN LIST ✓` (packed-green mono, row muted). Footer hint: `ADDING LISTS IT — GEAR DOES NOT MOVE UNTIL PACK-OUT`.
- Right list: suggestion band (`VOSGES 2025 LIST · 24 MATCH THIS DEPOT · ADD ALL / DISMISS`); groups by kind with mono headers + piece counts; shared rows get a `− ×2 +` stepper (32px, bordered radius 8); per-person rows get 24px inclusion circles + `×3`; every row has `✕` remove; TRIP-ONLY rows carry the amber badge; dashed add row `+ TRIP-ONLY ENTRY — NOT KEPT IN THE DEPOT, CLEARED AT CLOSE`; footer totals `34 GEAR · 61 PIECES · 18 PER-PERSON · 3 TRIP-ONLY · EST 48.2 KG` (weight tagged LATER; MVP variant drawn below). Keyboard: `↑↓ row · enter add/remove · P per-person · T trip-only`.

### 5a. Activation, over-claim & reopen (`Screens B` §02B, "Start pack-out — over-claim", "Reopen a closed trip")
- **Over-claim (stories 6, 32) — warns and allows, never blocks.** Two active trips cannot both take the same gear: Single gear is exclusive, counted bring-counts cannot sum past the owned-count, per-person gear is per participant. Drafts overlap freely — the check fires at **activation**, at three moments: adding gear to an *active* trip's list, starting pack-out on a draft, reopening a closed trip. Treatment: mono attention line `▲ 2 entries are already claimed by Alps 2026.` + conflict rows (name; mono fact `SINGLE · STILL OUT` / `×2 LISTED · ×1 OUT · OWNED ×2`; settle routes in accent — `REMOVE HERE` / `REMOVE ON ALPS` / `BRING ×1 HERE`) + body `Starting warns, never blocks. Nothing is removed unless you choose it.` Primary stays filled accent (`Start pack-out`) — **never a filled red button**; ghost Cancel.
- **Reopen a closed trip (stories 11, 32).** Closing destroys nothing — the packing arrangement is retained without effect — so every closed ledger row carries `REOPEN`. Confirm sheet in the delete-confirmation register (deliberate, not alarming; the primary stays accent since nothing was thrown away): `Reopen Tessin 2025?` · `It returns to Unpack exactly as it stood. Closing cleared nothing.` · mono `1 ENTRY STILL OPEN — HEADLAMP, K · ▲ LOST` · `Changing an outcome away from consumed offers to restore the owned-count and waits for the answer — a count corrected by hand is never rewritten.` If reopening would over-claim against another active trip, the same ▲ block appears in this sheet. **S6:** the confirm ships title + body only — the mono blocks are conditional on Entries and outcomes and return with them (information, not weight; the register is the confirm itself). The body is parameterised, `It returns to {phase} exactly as it stood.` — SET PHASE offers all four rows out of CLOSED and refusing them would contradict its own ANY ROW TAPPABLE; the closed row's `REOPEN` link targets Unpack, where the drawn sentence renders verbatim. The **confirmation itself** is neither S10's nor S11's: invariant 19 makes leaving `closed` a deliberate, confirmed act of the same weight as deleting a trip, and S6 is the slice that makes leaving `closed` possible at all — shipping the move without the confirm would have left an invariant violated for five slices. The phase word comes from a `name` field on the phase row rather than a casing transform, since no transform gets both `Pack-out` and `On trip` right.

### 6. Find — phone 393 (`Screens B`, "Find")
OFFLINE header state (amber dot). Title; 48px search field with amber border + caret and typed query; result count `4 MATCHES · ON-DEVICE INDEX` (Find keeps this read — it answers a question, it doesn't slice a list). Answer-first result card: gear header row (name + `PER-PERSON · ×3` + scope tag **`S8 · PIECES`**), then one row per piece: 28px person circle + whereabouts (mono 11, amber for trip / muted for home / attention `▲ LAST SEEN: TESSIN 2025`), right mini status chip (`PACKED`, `⌂ HOME`) or `RESOLVE` accent action. **Live today vs waits:** the per-person card is drawn final but waits on Pieces (S8) — until then per-person gear deliberately falls through to the plain 2-line row; the plain row, `⌂ HOME` chip and count line are live; the amber trip line and ▲ read arrive with the trip/outcome slices. The unaccounted state is **MVP** (stories 3, 11): `lost` writes nothing to the depot and keeps the gear's home; it reads identically on Gear detail and in the Depot WHEREABOUTS column. Plain matches = standard 2-line rows (the same GearRow, meta slot = `⌂` path; `⌂ LOOSE` when it lives nowhere). RECENT = mono 44px rows. Tab bar, FIND active.

### 7. Unpack & close — phone 393 (`Screens B`, "Unpack and close")
Header `‹ ALPS 2026`. Title `Unpack`; count line `● 52/61 BACK` vs `6 OPEN · 2 CONSUMED · 1 LOST`; progress bar; filter pills `○ OPEN ONLY ✕` (selected) + `BY DESTINATION`.
- **Grouped by home destination** (Attic 6/8 BACK, Kelder, Hal, No home slot) so the user walks the house once. Row meta = return path `→ CRATE B · ×2`.
- Pill verbs: **BACK** (filled circle, packed style) · **OPEN** (empty circle, muted — one word for the unresolved state everywhere; never "out") · **CONSUMED** (dashed border `#6A7161`, no icon; meta e.g. `×2 TAKEN · ×2 EMPTY`) · **LOST** (▲ attention, meta `▲ NOT SEEN AT UNPACK`) · trip-only **CLEARS** (dashed, meta `CLEARED AT CLOSE`). Marking **BACK hands the gear home there and then** (story 11) — whereabouts returns to home immediately, not at close.
- Close card (surface): summary mono `52 BACK · 2 CONSUMED · 1 LOST · 6 OPEN`; full-width 48px button **gated** — disabled style (bg `#28312A`, muted text) reading `Close trip — 6 open` until open = 0, then accent `Close trip`; hint `BACK WRITES HOME AT THE TAP — NOT AT CLOSE. CLOSE WHEN OPEN = 0. CONSUMED REDUCES DEPOT COUNT. LOST KEEPS THE HOME SLOT, STAYS SEARCHABLE.`

### 8. Sign in — the signed-out shell, phone 393 + desktop 1024 (`Screens C`, "Sign in — idle / passkey ceremony / failed / offline", "Sign in desktop")
Purpose: the shell that loads offline from the service worker with zero data. **No username, no password, no email — one button.**
- Layout: centred stack — mark 72×57, wordmark Bricolage 700 32, ledger line 13 muted `The household's gear ledger.` Thumb zone: 48px accent `Sign in`, ghost `No passkey on this device?` (opens the explainer sheet, §15), mono 10.5 faint footer `Sign-in is a passkey. No passwords, no email.`
- **Ceremony state:** shell dims under `rgba(15,19,15,.62)`, button drops to disabled style. The OS passkey sheet (including the browser's own cross-device QR flow) is an **external surface — recreate nothing of it**, and never replace it with a spinner screen.
- **Failed:** mono 12 attention `▲ Sign-in did not complete. Nothing changed.` + secondary `Try again, or ask a household member for a device link.`; button returns to idle.
- **Offline:** header amber dot + `OFFLINE`; mono 12 muted `Offline. Sign-in needs a connection.`; button disabled (bg `#28312A`, text `#5E6857`); the explainer link stays enabled (needs no network).
- Desktop: signed out there is no sidebar — a centred 340px column on bg/base; focus ring 2px amber on the primary (Enter signs in); `BUILD 7C39F2A` bottom-right, mono faint. The shell keeps this structure across all five modes; the layout ladder applies to signed-in surfaces only.

### 9. Join — phone 393 (`Screens C`, "Join — confirm / name yourself / success / dead end")
- **Confirm:** title `Join Veldkamp?`; ledger card rows (LABEL mono 8.5 faint + value 16/600): HOUSEHOLD `Veldkamp` · YOU JOIN AS `Els` · INVITED BY `MARK · 2026-08-21` (mono value); chips `EXPIRES IN 6 d` + `SINGLE USE`. Directly above the button: mono 12 `Opening this link changed nothing yet.` (rule: nothing is consumed by opening a link). Primary `Join as Els`; ghost `No passkey on this device?` beneath it (the S3.5 departure door — §10); quiet line `Not Els? Ask Mark for a new link.` Confirm → OS passkey-creation sheet → success.
- **Name yourself** (only for a brand-new household's first login, where the Person is created on join): body `This link starts a new household. Its first login is yours.`; one field YOUR NAME (48px, drawn focused: amber border + ring); mono `Household and depot start empty.`; primary `Continue`.
- **Success + first sync — the app's only unavoidable loading screen:** title `Signed in.`; `Els · Veldkamp` — tagged **`API FIELD`**, with the confirm frame's `YOU JOIN AS` and `INVITED BY`: all three are blocked on the endpoints returning the person name; recommended endpoint change (an invite names its person by definition — ubiquitous language); until then the line is omitted, never faked. FIRST SYNC — ONE-TIME card, **determinate ops fold, never a spinner**: `36%`, 6px bar, mono `OP 4,215 OF 11,562 FOLDED`, copy `This device folds the household's history once. After this it starts instantly and works offline.` **The dash state is drawn** (frame "First sync — counting"): the denominator arrives with the first page, so for one round trip the honest line is `OP 0 OF — FOLDED`, 0%, empty bar; screen readers hear "Counting"; the CTA starts gated at 0% and only ever enables once; a household with nothing to fold sees the card flash once — accepted. CTA gated like the close button: muted `Open the depot — folding 36%` until the fold completes (a new device folds the whole op log before it can show anything — sync-protocol §7, paged pull; cost grows with the household). **Resumable:** the bootstrap cursor is persisted per page, so a dropped connection shows `FIRST SYNC — PAUSED` · `OP 4,215 OF 11,562 · CURSOR KEPT` · quiet `RETRY NOW` · `Connection dropped. It continues from op 4,215 when the line returns — nothing restarts.` (frame "First sync — resumes"). Not an error state: the offline dot is the only amber, no ▲. The same fold runs on any freshly linked device (flow D).
- **Dead end — one screen, swapped fact line.** Constant title `Invite not valid.`; the line swaps: EXPIRED `This invite expired 2026-08-18. Invites last 7 days.` · USED `This invite was used 2026-08-19 14:32. Invites are single-use.` · UNKNOWN `This server does not know this link.` Body `Ask a household member for a new one. Nothing was used up by opening this.`; bordered `Open sign-in`. **No attention color** — the link is done, nothing of the user's is wrong.

### 10. Continue without a passkey — phone 393 (`Screens C`)
The compatibility path, drawn deliberately first-class: same accent primary (`Continue as Els`), no amber, no ▲, no "however". Two body lines: `No passkey is made here. It stays signed in anyway — nothing is limited.` and `A passkey added later, on any device that supports one, makes future sign-ins self-service.` Mono fact `You stay signed in until you sign out.` The **standing nudge** is the Account ▸ Passkeys empty state — `None on this login.` + body + bordered `Add a passkey on this device` (rendered only where the device supports passkeys) — a quiet section state, never a toast or badge.

- **Departure — S3.5, two parts, one reason.** The boards draw this screen's *destination* but not its *door*: every trigger `auth-design.md` §5 lists is a failure to make a passkey. That is not enough. On the Android builds that motivated the slice every capability check passes — the ceremony succeeds, by putting a credential in the platform store, which is precisely the store the household declined. Detection cannot tell "this device cannot" from "this household will not"; only the person holding the phone can. So (1) the **join confirm frame gains a ghost line beneath its primary**, in the sign-in screen's existing words `No passkey on this device?`, leading here as a deliberate choice — the automatic fall-through is unchanged, this is a door beside it. And (2) the first body line reads **`No passkey is made here.`** rather than `This device cannot make one.`, because the original is now sometimes false and this screen's whole discipline is that it states plain facts without amber, ▲ or apology. **Two doors, two destinations, deliberately:** the same words on the *sign-in* screen open the explainer sheet (§15), because there is no invite in hand and the answer is "ask a household member"; on the *confirm* frame they lead here, because a secret is in hand and the path is available now. Do not wire both to one. Everything else about the anatomy is untouched. Both parts are on the boards. Recorded per the precedent set by §3b/§3c.

### 11. Account — phone 393 + desktop 1024 (`Screens C`, "Account phone", "Account desktop")
The fourth destination beside Depot · Trips · Find, reached from the avatar — not a tab, so the 3-tab bar is untouched; pushed screen with `‹ DEPOT` + sync line.
- Section order (frequency order): **YOU** (40px accent-border circle, name 17/600, `VELDKAMP HOUSEHOLD` mono 11) → **PASSKEYS** (rows: coarse label 16/600 — `Pixel 9`, `YubiKey, desk drawer` — + mono 11 `ADDED 2026-03-02 · LAST USED TODAY`; `REMOVE` attention text; bordered 48 `Add a passkey on this device`) → **DEVICES** (mono `3 devices signed in.`; `All devices ›` row with `THIS ONE: FIREFOX ON ANDROID`; bordered 48 `Sign in on another device` — the device-link entry point) → **PEOPLE & LOGINS** (row + `2 OF 3 PEOPLE HOLD A LOGIN`) → footer (surface): `SIGN OUT` attention text left, `BUILD 7C39F2A · 2026-08-21` mono faint right.
- Desktop: a pane inside the 216px-sidebar shell, **never a modal**. The sidebar is **the** app nav — settled R3 as the one 216px sidebar on every desktop screen (§2 now matches; the Depot rail is retired): DEPOT 128 / TRIPS 3 / FIND, ACCOUNT the active row pinned bottom (22px avatar circle + label, bg `#28312A`; the circle and row are muted on other screens), sync line beneath. At Split, the rail's bottom avatar opens Account the same way (§3a). Content: 2-col cards; the phone's summary rows unfold — full device list and all three people inline; dense 40px buttons.

### 12. Devices — phone 393 (`Screens C`, "Devices" + two confirm sheets)
- Rows ≥56: coarse label 16/600 (`Firefox on Android`, `Edge on Windows`, `Safari on iPad`) — no IPs, no fingerprinting; `THIS DEVICE` chip (accent badge) on the current one; mono 11 meta `LAST SEEN 2026-08-19 14:32` / `SIGNED IN 2026-03-02 · NO PASSKEY HERE` (a passkey-less device is a plain fact, not a warning). Timestamps render in the device's local time, no zone suffix (S5 — §13's rule). Per-row `SIGN OUT` in attention color as **text, never a filled red button** (the RETIRE rule). Footer hint `SIGNING OUT A DEVICE REACHES IT AT ITS NEXT SYNC.`
- **Remote confirm sheet:** title `Sign out Safari on iPad?`; body `It loses access at its next sync. Everything already synced stays with the household.`; bordered-attention 48 `Sign out device` + ghost Cancel.
- **Sign out this device:** the only auth action that can discard work, therefore the only one carrying ▲: mono attention `▲ 4 changes not yet synced. Signing out clears them.` (exact count; the line is omitted entirely when nothing is unsynced) + body `Local data is removed from this device. Synced work stays with the household.`; `Sign out and clear` + Cancel. Afterwards: local data cleared → sign-in screen.

### 13. People & logins — phone 393 (`Screens C`, "People and logins — one login / two logins / login state unknown / revoke a login")
Count line `2 of 3 people hold a login.` Rows: 30px person circle — **accent border `#93BC9F` = holds a login, control border `#47523F` = none, and (S5) a third appearance that is a withdrawal, not a claim: ring absent + initial ink/muted = login state not loaded.** The ring is the statement "login state is known"; when it cannot be made it is withdrawn, never restated in a third colour — nothing new to flatten in any theme; dashed stays the add affordance; fill never appears on account surfaces (fill belongs to packing). Light values: accent `#35523F`, control `#BBB6A2`, withdrawn = no ring. **Unknown is screen-level, never per-row (S5):** who holds a Login is a server fact, so a failed load falls the list back whole — no meta lines, no right column, count line `3 people.`, and one quiet mono line directly under it: `Login state could not be loaded. Check your connection.` (Account's existing words); the visibly withdrawn circles tie the line to the rows. Name 16/600 (+ `YOU` badge on self), mono 11 meta — the full set (S5 adds the third): `SIGNED IN · 2 DEVICES` · `SIGNED IN · 1 DEVICE · LAST SEEN 2026-08-20 19:04` · **`LOGIN · NO DEVICE SIGNED IN` (S5)** — a Login whose every Device is signed out; `SIGNED IN · 0 DEVICES` would be false in both words; the row keeps `DEVICE LINK ›` + `REVOKE`, anatomy unchanged (a locked-out member is a fact, not an alarm; the way back in sits right where the lockout is stated) · `NO LOGIN · JOINS TRIPS AS PARTICIPANT` · `INVITE OUT · SINGLE USE`. **Timestamps render in the device's local time, no zone suffix (S5)** — UTC rejected (false for every reader of a one-timezone household), relative forms rejected (the ledger writes dates as data); §12's device rows follow the same rule. Right side: `INVITE ›` accent text for a person without a login — **it mints on arrival (S5):** the handover screen is the confirmation; backing out leaves the row reading `INVITE OUT` with `REVOKE` one tap away, and re-issue supersedes, so nothing invisible accumulates. `DEVICE LINK ›` + `REVOKE` on another person's login (any member may issue/revoke for anyone); the **own row gets only `›`, which opens Devices (S5)** — it expands the meta beside it — **and is dropped in the Desktop inline variant**, where the Devices card sits two rows above; your exit is SIGN OUT, not self-revocation. **Revoking a Login confirms; revoking an Invite does not (S5)** — confirms scale with consequence, not with label: an invite is a link and never data, and a mistaken kill is one `INVITE ›` from repair. The sheet: `Revoke Els's login?` · `Els's devices lose access at their next sync. Everything Els recorded stays with the household.` · bordered-attention `Revoke login` + `Cancel` (initial focus), Escape closes, no scrim dismissal, **no ▲** — §12's rule stands: only sign-out-this-device can discard work. The outstanding-invite row is the collapsed handover screen: expiry chip + `REVOKE` — **`REOPEN ›` is retired (S5):** the invite secret is stored hashed and the plaintext lives only in the link, so nothing — server or client — can reopen a closed handover; re-handing is `REVOKE`, then the same row's `INVITE ›` (the second step is the verb the goal names, drawn on the board). Quiet mono `EDIT` toggle carries `RENAME`, never `REMOVE`; **EDIT replaces the right column (S5)** on every row, own row included, so actions and editing never compete for the slot and nothing can be revoked mid-edit. Dashed `+ NEW PERSON` row at the bottom. Alphabetical order, identical anatomy, no owner badge — equality is typographic law. **S4 ships this screen's People half and nothing else** (`/account/people`, reached from Account's `PEOPLE` section — the row S3.5 deliberately left out, now put in by the same rule that kept it out, because it leads somewhere real). Live: the 30px circle with its initial, the name, the `YOU` badge, alphabetical order, the count line as `3 people.`, the dashed `+ NEW PERSON` row, and **rename behind an EDIT mode** — the quiet mono toggle §3c settled — carrying `RENAME` and never `REMOVE`, since a Person is never removed and the screen says so in EDIT mode rather than leaving the absence to be inferred. At Desktop the list unfolds inline into Account's card and `/account/people` redirects back, exactly as `/account/devices` does. **Three things S5 owes, written down so they are a debt and not a gap:** (1) the row's **meta slot is empty** — every line the board draws there is login state and `GET /auth/logins` is S5's; (2) the circle **carries no login encoding**, drawing the control border with no meaning attached, because drawing every circle as "no login" would render the joiner — who demonstrably holds one — as having none, and stating something false is worse than stating less; (3) the section and screen are titled **`PEOPLE`**, and S5 renames both to `PEOPLE & LOGINS` and adds the count line's second clause, the right column (`INVITE ›`, `DEVICE LINK ›`, `REVOKE`), the outstanding-invite row and the own row's `›`.

### 14. Invite issued — phone 393, one screen for three doors (`Screens C`, "Invite issued — join / device link / device link for another person")
Anatomy (invite card): QR on a light tile (`#F0EBDD`, radius 8, 126px incl. 10px quiet zone) · the link in the input well (`#0F130F`, mono 11.5, wrapped, `user-select:all`) · bordered 48 `Copy link` · expiry chip + mono `SINGLE USE` · revoke attention text (kills the link, never data). **Revoke labels settled (S5):** `REVOKE INVITE` on the join door, `REVOKE LINK` on both device-link doors — a join invite and a device link are two different things in the vocabulary; the old decision note's "only title, lead, expiry and fact change" was the wrong half of §09's self-contradiction and is corrected on the board. **The back link follows the route in (S5):** `‹ ACCOUNT` only for your own device link; `‹ PEOPLE & LOGINS` for the join invite and for another person's device link. **Minted on mount (S5):** `INVITE ›` / `DEVICE LINK ›` mint on arrival; this screen is the confirmation — backing out leaves the credential visible on its row with `REVOKE` beside it; no auto-revoke on back (it would kill a link the user may already have copied). **(S3.5)** The example link in both wells reads `https://foerier.veldkamp.net/join#…`, corrected in the boards from the original `/j/…` (join) and `/d/…` (device link) shapes: `/d/…` never existed as a real path, and `JoinContainer` unifies both purposes on the one `/join#<secret>` route (§9's own confirm-frame anatomy already assumed this).
- **Join invite:** `‹ PEOPLE & LOGINS`; title `Invite for Els`; lead `Hand it over yourself — foerier sends no mail.`; chip muted `EXPIRES IN 6 d`; fact `It creates a login for Els. Nothing else can use it.`; `REVOKE INVITE`.
- **Device link (own):** `‹ ACCOUNT`; title `Sign in on another device`; lead `Open this on the other device. It signs that device in as you, Mark.`; chip amber `EXPIRES IN 58 min`; fact `The link is the credential. Treat it like a key.`; `REVOKE LINK`.
- **Device link for another Person (S5):** `‹ PEOPLE & LOGINS`; title `Device link for Els`; lead `Open this on Els's device. It signs that device in as Els.`; chip amber, 1 hour; fact and revoke label identical to the own-device door. Deliberately no distinct treatment: one anatomy is this screen's law, the words carry the difference, and the amber 1-hour chip is already the hottest element — the fact line stays word-for-word (the security model, stated once, in the same words, wherever it applies).
- **Expiry chip** (component): existing chip anatomy (radius 999, 1.5px stroke, mono 10/600); muted (`#47523F`/`#97A08C`) while ≥1 hour remains, amber (`#E2A65B` + `rgba(226,166,91,.08)` tint) under an hour; live count, minute granularity.

### 15. Explainer sheet + session lost — phone 393 (`Screens C`, "Explainer sheet", "Session lost")
- **Explainer** (opens from `No passkey on this device?`): the sheet primitive — radius 16 top, bg/surface, 1px rule top edge, 36×4 grabber `#47523F`, dim `rgba(15,19,15,.62)`, padding 10/20/24. Title 22 Bricolage; two paragraphs 14/21, first ink/primary, second ink/muted: (1) ask a signed-in household member for a device link — People & logins ▸ your name ▸ Device link — opened here it signs this device in; (2) if yours is the only login and it is signed in nowhere, ask whoever runs your server for a new join invite — **the only place the Maintainer is ever named**. Ghost `Close` 48.
- **The sheet primitive's dismissal rule, settled at the Radix conversion** ([spec](../specs/2026-08-29-radix-conversion.md) §3.1): **a picker dismisses on the scrim; a decision does not.** The boards draw the dim but never say what tapping it does, and the eleven surfaces that shipped before the conversion disagreed — three confirms could only be left through their own Cancel, two could be tapped away mid-flight. Escape closes both kinds. A stray tap on the dim area is not a decision, and `Sign out this device?` in particular can be part-way through `clearLocalData()` when it lands. Every overlay also gained a focus trap, focus return and a scroll lock, which none of them had.
- **Session lost** (token revoked/expired, discovered by the sync client): the sign-in shell plus one extra mono 12 **muted** line above the button — `Signed out on this device. 12 changes saved here and not yet synced.` Fact weight: no ▲, no red, no modal, no banner. In-app the only trace is the existing sync line reading `SIGNED OUT · SAVED ON DEVICE`; Depot/Trips/Find keep working offline. Signing back in flushes the queued changes automatically. **This must never read as data loss.**

## Auth vocabulary & rules
Use these words exactly; never "account" (except the tab name), "user", "profile", "log in", "admin", or "registration".
- **Household** — the people, depot and trips that belong together; the privacy boundary. **Person** — a household member who may join trips; most never sign in. **Quartermaster** — a Person who manages the Depot and outfits Trips; all identical in power, no hierarchy. **Login** — a Person's ability to sign in; one Person holds at most one. **Passkey** — a credential proving a Login; a Login may hold several or none. **Device** — one signed-in browser installation; listed and revocable. **Invite** — a single-use, short-lived link: a **join invite** creates a Login for a Person; a **device link** signs an existing Login in on another Device. **Maintainer** — whoever runs the server; not a role in the product.
- Verbs: *sign in / sign out* (never "log in/out"), *join*, *invite*, *revoke*, *add a passkey*, *issue a device link*.
- Rules the UI must keep visible: no passwords or email anywhere (foerier sends no mail) · no usernames — one button · devices without passkeys are equals · all Quartermasters equal · offline work is never lost to auth, and every session-ending screen says so · invites always state single-use + remaining time · opening a link does nothing until confirmed · auth exists on exactly two surfaces (signed-out shell, Account) and never interrupts Depot, Trips or Find.

## Interactions & Behavior
- Tap a status pill → next state (○ → ◐ → ● → ○). Long-press a per-person cluster → apply to all people. UNDO appears after every change.
- Moving a container moves its contents in one action; containment is a path (`ATTIC ▸ SHELF ▸ CRATE B`), one level shown at a time.
- Adding to a trip lists it; nothing moves until pack-out. Consumed decrements depot quantity quietly; Lost writes nothing to the depot, keeps the home slot, and stays searchable; trip-only entries are cleared at close. Marking `back` mid-unpack returns whereabouts home immediately.
- Over-claim check at activation only (adding to an active trip · starting pack-out · reopening): **warns and allows** — ▲ + attention colour + settle routes; never a block, never a filled red button, never discarded work. Drafts overlap freely.
- Reopening a closed trip restores its Unpack exactly as it stood (closing clears nothing). Changing an outcome away from `consumed` **offers** to restore the owned-count and waits for confirmation — a hand-corrected count is never rewritten silently.
- Phase model: `DRAFT → PACK-OUT → ON TRIP → UNPACK → CLOSED` — set by a quartermaster, never by dates or counts, movable in **both directions**. The phase chip opens SET PHASE; the CTA names the current phase verb but is only a suggestion. Day count runs from the phase change.
- Offline is normal, never a blocking dialog: sync state is one quiet header line (sage dot SYNCED / amber dot OFFLINE). Search, edit, pack fully on-device.
- Desktop keyboard: `↑↓` row, `space` select, `/` focus search, `enter` open/add, plus builder shortcuts above.
- Hover states (desktop): rows/nav items `background:#1F2620`. Focus: 2px amber ring.
- **Auth:** one button → OS passkey sheet (incl. the browser's cross-device QR — external, recreate nothing). Failure returns to idle with `▲ Sign-in did not complete. Nothing changed.` Invites: join 7 days, device link 1 hour, both single-use; opening a link changes nothing until confirmed; REVOKE kills the link only. Remote sign-out reaches a device at its next sync; sign-out-this-device clears local data and warns with the exact unsynced count. Session loss is discovered by the sync client, never interrupts a task, and queued work flushes on the next sign-in.

## State Management
- Entities: Gear (kind item/container, owner shared|person, owned-count, home path, tags — **no per-unit identity, no condition, no purchase year**), Trip (optional dates, people incl. non-packing participants, phase — set by hand, both directions — gear list with per-person allocation, trip-only entries), LedgerEvent (append-only: packed/listed/moved/back/lost/consumed/retired, timestamp, actor — **LATER**, story 33, derived from the change log).
- Split whereabouts: a counted gear's quantity splits across worlds (×1 home, ×1 on trip — both shown). Per-person **Pieces** exist only in trip contexts.
- Local-first store + on-device search index; background sync; last-synced timestamp surfaced in sidebar/header. First sync on a new device is a blocking, determinate fold of the household's op log (ops folded / ops known) with the bootstrap cursor persisted per page — it resumes, never restarts (sync-protocol §7).
- Close gating: `open === 0` (every piece resolved to back/consumed/lost) enables Close. `back` writes home immediately, mid-unpack. Closing retains the packing arrangement without effect, so a closed trip can reopen to Unpack as it stood; outcome edits away from `consumed` offer (never force) an owned-count restore.
- Auth entities: Household (privacy boundary — nothing crosses it), Person (holds at most one Login), Login (holds Passkeys and Devices; all Logins have identical powers — no roles in data), Passkey (coarse label, added, last-used), Device (coarse label, last-seen, token), Invite (kind join|device-link, single-use, expiry, revocable). The offline queue survives a revoked token; it flushes after the next successful sign-in — only an explicit "sign out this device" clears it.

## Assets
No raster assets. The logo is inline SVG (28×22 viewBox): soft-taper duffel outline (`M8,6 L20,6 Q25.5,6.5 25.5,13 Q25.5,19.5 20,20 L8,20 Q2.5,19.5 2.5,13 Q2.5,6.5 8,6 Z`, stroke ink 1.7–1.9), two vertical seams at x=10/18 (stroke 1.3), amber handle arc (`M11,5.6 Q14,1.6 17,5.6`, round caps). Seams drop at sizes ≤20px. Wordmark: lowercase `foerier`, Bricolage Grotesque 700, mark-to-text gap 0.35×cap height, clearspace = handle height. Known open nudge: wordmark should sit slightly higher and gain a touch more letter-spacing — treat `Logo Round 4 - Radius Sweep.dc.html` as the current reference. Icons (tab bar, search, checks) are simple inline SVG strokes at 1.6–1.9px.

## Files
- `Foundations.dc.html` — principles, logo spec, type scale, color tokens (dark+light), status grammar + the decided hybrid comparison, spacing/measured redlines, voice.
- `User Flows.dc.html` — flow maps: Depot entry → gear detail; Trip creation → list builder → packing → unpack & close; Find (offline); auth flows A–F (join, sign in, invite a person, device link, manage devices, session lost) with OS/browser steps marked external.
- `Components.dc.html` — component sheet: rows (§03 now carries the canonical **GearRow variant map** — 2-line / 1-line / table-44 picked by @container; Find = meta-slot swap; builder row = separate component; extraction into `ui/` lands at S3 per frontend-design §5), chips, per-person indicators, the **slice bar at S3 ship state** (§04: 36px chips settled, dimension-only ghosts, GROUP BY `NONE · KIND`, sort menu + collapsed readout, dashed future-dimension ladder, persistence rules), containment tree + the settled home-picker rows and Add-gear atoms (§05, R2 — the old blocked-rows MOVE mock is retired), quantity controls, empty/loading states, packing actions; §06 the settled tag chip; §09 auth atoms (passkey/device rows, person-with-login indicator, sign-in button states, expiry chip, invite card, auth explainer sheet); §10 the scope tag (LATER + slice-numbered + API FIELD variants); §11 the whereabouts-vocabulary ruling.
- `Screens A - Packing + Depot.dc.html` — layout-mode ladder (five em-based modes), Packing (container/person, phone), Depot (desktop + phone + grouped-by-kind phone + Roomy 540 + Split 900 two-pane), Add gear F1 (§06, redrawn R2: fresh + mid-sitting-with-keyboard 393, Roomy 540 counted gate, Split 900), Home picker sheet (§07, redrawn R2: pick mode + edit mode, MOVE variant, remove confirm, empty household).
- `Screens B - Detail + Trips + Find + Unpack.dc.html` — Gear detail (settled tag chips + `+ tag`), tag pickers (§01B: gear-detail sheet + slice-bar popover), Trips, Gear list builder (desktop) + MVP header/footer variants, over-claim & reopen sheets (§02B), Find (S8 scope tags), Unpack & close.
- `Screens C - Auth + Account.dc.html` — sign-in shell (idle/ceremony/failed/offline + desktop), join (confirm/name yourself/success/dead end), first-sync fold (blocking, determinate + resumed state), continue without a passkey, Account (phone + desktop), Devices + both confirm sheets, People & logins (four frames: one login / two logins / login state unknown / revoke-a-login confirm), invite issued (three doors: join + own device link + device link for another person), explainer sheet, session lost.
- `Direction C - Field Tag.dc.html` — the chosen art-direction board (context).
- `Logo Round 4 - Radius Sweep.dc.html` — latest logo iteration (reference for the mark).
- `support.js` — runtime the `.dc.html` files need to render; keep it in the same folder.
